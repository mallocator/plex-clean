{
  "name": "plexclean",
  "version": "1.1.0",
  "description": "A script that triggers when a video finished playing in plex and then writes a files into a local dir to be consumed by another script. (Yes this was supposed to do something else originally)",
  "main": "clean.js",
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "multer": "^1.4.5-lts.1",
    "plex-api": "^5.3.2"
  }
}
